name: Check

on:
  pull_request:
  push:
    branches: [main]

jobs:
  repo-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run repo checks
        run: ./tools/check-repo.sh
