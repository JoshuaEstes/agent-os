# Core

Core contains the stable concepts and boundaries of Agent OS.

Keep this folder small. If a document describes repeatable action, it probably belongs in `workflows/`. If it describes an installable worker, it belongs in `agents/`.
